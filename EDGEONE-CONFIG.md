# EdgeOne 部署配置

## 构建配置

在 EdgeOne 控制台配置以下参数：

### 基础配置

- **框架预设**：Other（其他）
- **Node.js 版本**：18.x 或更高
- **包管理器**：npm

### 构建命令

```bash
npm run build
```

### 输出目录

```
dist/portal
```

### 安装命令（默认）

```bash
npm install
```

---

## 环境变量

无需配置环境变量。

---

## 构建说明

### 构建流程

1. **安装依赖**：`npm install`
2. **执行构建**：`npm run build`
   - 步骤 1：构建 VitePress 门户
   - 步骤 2：构建所有 Slidev 课程（增量构建）
3. **输出目录**：`dist/portal`

### 构建时间

- 首次构建：约 1-2 分钟（需要构建所有课程）
- 增量构建：约 30-60 秒（只构建修改的课程）

### 构建缓存

系统会自动使用 `.buildcache` 目录缓存构建结果，加速后续构建。

---

## 常见问题

### Q: 构建失败，提示找不到命令？

A: 确保 Node.js 版本为 18 或更高。

### Q: 构建时间过长？

A: 首次构建需要构建所有课程，后续构建会使用增量构建，速度会快很多。

### Q: 课程没有更新？

A: 
1. 检查 EdgeOne 构建日志，确认构建成功
2. 清除浏览器缓存
3. 在 EdgeOne 控制台刷新 CDN 缓存

---

## 推荐配置

### CDN 缓存规则

| 路径 | 缓存时间 | 说明 |
|------|---------|------|
| `/*.html` | 5 分钟 | HTML 文件 |
| `/assets/*.js` | 1 年 | JS 文件（带哈希） |
| `/assets/*.css` | 1 年 | CSS 文件（带哈希） |
| `/courses/**/*` | 1 天 | 课程内容 |
| `/assets/fonts/*` | 1 年 | 字体文件 |

### HTTPS 配置

- 启用 HTTPS
- 开启 HTTP/2
- 开启强制 HTTPS 跳转

---

## 验证部署

部署成功后，访问你的域名：

1. 首页应该显示课程列表
2. 点击"🎯 进入演讲模式"应该能正常打开课程
3. 搜索、分类、标签功能正常工作
